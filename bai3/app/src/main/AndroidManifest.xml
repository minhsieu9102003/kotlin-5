<?xml version="1.0" encoding="utf-8"?>
<!-- Ensure the package attribute matches your application's package name -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication2">

    <!-- Application element -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.MyApplication2"> <!-- Application theme -->

    <!-- MainActivity declaration -->
    <activity
        android:name=".MainActivity"
        android:exported="true"> <!-- Required for activities with intent filters on Android 12+ -->

        <!-- Intent filter for the launcher activity -->
        <intent-filter>
            <action android:name="android.intent.action.MAIN" /> <!-- Entry point of the app -->

            <category android:name="android.intent.category.LAUNCHER" /> <!-- Makes this activity the launcher -->
        </intent-filter>
    </activity>

    <!-- Declare other activities if any -->

</application>

    </manifest>
